# версии зафиксированы: диапазоны делают сборку невоспроизводимой — деплой на Railway
# мог бы молча подтянуть новую мажорную версию и сломать уже работающего бота.
# Здесь именно те версии, на которых бот проверен локально.
aiogram==3.30.0
aiohttp==3.14.3
aiosqlite==0.22.1
python-dotenv==1.2.2
